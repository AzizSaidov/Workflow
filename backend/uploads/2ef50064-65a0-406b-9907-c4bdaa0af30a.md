# Workflow — Style Guide
> Этот файл — основа для всех страниц. Агент должен строго следовать этому гайду.

---

## Цвета

```css
/* DARK THEME */
--bg:           #07070E;   /* фон страницы */
--bg-card:      #0D0D18;   /* фон карточек */
--bg-input:     #0D0D18;   /* фон инпутов */
--border:       rgba(255,255,255,0.07);  /* обводка карточек */
--border-hover: rgba(127,119,221,0.45); /* обводка при hover */
--accent:       #7F77DD;   /* основной акцент — фиолетовый */
--accent-green: #1D9E75;   /* зелёный акцент */
--accent-light: #AFA9EC;   /* светлый фиолетовый */
--accent-teal:  #5DCAA5;   /* бирюзовый */
--text-primary: #FFFFFF;   /* основной текст */
--text-secondary: rgba(255,255,255,0.38); /* второстепенный текст */
--text-muted:   rgba(255,255,255,0.22);  /* приглушённый текст */

/* LIGHT THEME */
--bg-light:        #F0EFFB;
--bg-card-light:   #FFFFFF;
--accent-light-th: #4038B2;
--accent-green-light: #108C67;
--text-primary-light:   #0F0E1A;
--text-secondary-light: #5A5570;
--text-muted-light:     #7A758E;
```

---

## Шрифты

```html
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;700;800&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet">
```

```css
font-family: 'DM Sans', sans-serif;   /* весь текст */
font-family: 'Syne', sans-serif;      /* заголовки, логотип, цифры */
```

---

## Иконки

```html
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@tabler/icons-webfont@2.44.0/tabler-icons.min.css">
```

```html
<!-- Использование -->
<i class="ti ti-briefcase"></i>
<i class="ti ti-code"></i>
<i class="ti ti-arrow-right"></i>
<i class="ti ti-star-filled"></i>
<i class="ti ti-clock"></i>
```

---

## Звёздный фон (canvas) — копировать на КАЖДУЮ страницу

```html
<canvas id="bgCanvas" style="position:fixed;top:0;left:0;width:100%;height:100%;z-index:0;pointer-events:none;"></canvas>
```

```javascript
// DARK: мерцающие звёзды + падающие звёзды
// Вставить в конец body каждой страницы
(function(){
  const canvas = document.getElementById('bgCanvas');
  const ctx = canvas.getContext('2d');
  function resize(){
    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight;
  }
  resize();
  window.addEventListener('resize', resize);

  const stars = [];
  for(let i=0;i<220;i++){
    const r=Math.random();
    stars.push({
      x:Math.random()*canvas.width, y:Math.random()*canvas.height,
      r:Math.random()*1.4+0.2,
      ba:Math.random()*0.65+0.1, a:0,
      ts:Math.random()*0.022+0.004, to:Math.random()*Math.PI*2,
      vx:(Math.random()-0.5)*0.05, vy:(Math.random()-0.5)*0.035,
      col:r>0.86?'#7F77DD':r>0.73?'#5DCAA5':'#ffffff'
    });
  }
  const shooters=[];
  for(let i=0;i<5;i++) shooters.push({on:false,t:60+i*100,x:0,y:0,vx:0,vy:0,p:0,a:0});
  function spawnS(s){
    s.on=true; s.p=0;
    s.x=Math.random()*canvas.width*0.7;
    s.y=Math.random()*canvas.height*0.4;
    const ang=Math.PI/6+Math.random()*Math.PI/7, sp=7+Math.random()*6;
    s.vx=Math.cos(ang)*sp; s.vy=Math.sin(ang)*sp; s.a=1;
  }
  let t=0;
  function frame(){
    ctx.clearRect(0,0,canvas.width,canvas.height);
    t+=0.016;
    for(let i=0;i<stars.length;i++){
      const s=stars[i];
      s.x+=s.vx; s.y+=s.vy;
      if(s.x<0)s.x=canvas.width; if(s.x>canvas.width)s.x=0;
      if(s.y<0)s.y=canvas.height; if(s.y>canvas.height)s.y=0;
      s.a=s.ba*(0.4+0.6*Math.sin(t*s.ts*60+s.to));
      ctx.beginPath(); ctx.arc(s.x,s.y,s.r,0,Math.PI*2);
      ctx.fillStyle=s.col; ctx.globalAlpha=s.a; ctx.fill();
      if(s.r>1.0&&s.a>0.3){
        const gc=s.col==='#7F77DD'?'rgba(127,119,221,':'rgba(93,202,165,';
        const g=ctx.createRadialGradient(s.x,s.y,0,s.x,s.y,s.r*3.2);
        g.addColorStop(0,gc+'0.28)'); g.addColorStop(1,'transparent');
        ctx.beginPath(); ctx.arc(s.x,s.y,s.r*3.2,0,Math.PI*2);
        ctx.fillStyle=g; ctx.globalAlpha=s.a*0.5; ctx.fill();
      }
    }
    for(let i=0;i<shooters.length;i++){
      const s=shooters[i];
      if(!s.on){s.t--;if(s.t<=0)spawnS(s);continue;}
      s.x+=s.vx; s.y+=s.vy; s.p++;
      s.a=Math.max(0,1-s.p/60);
      const tx=s.x-s.vx*9, ty=s.y-s.vy*9;
      const gr=ctx.createLinearGradient(tx,ty,s.x,s.y);
      gr.addColorStop(0,'rgba(255,255,255,0)');
      gr.addColorStop(0.5,`rgba(210,206,255,${s.a*0.5})`);
      gr.addColorStop(1,`rgba(255,255,255,${s.a})`);
      ctx.beginPath(); ctx.moveTo(tx,ty); ctx.lineTo(s.x,s.y);
      ctx.strokeStyle=gr; ctx.globalAlpha=s.a; ctx.lineWidth=1.8; ctx.stroke();
      ctx.beginPath(); ctx.arc(s.x,s.y,2.2,0,Math.PI*2);
      ctx.fillStyle='#fff'; ctx.globalAlpha=s.a; ctx.fill();
      if(s.p>65||s.x>canvas.width||s.y>canvas.height){
        s.on=false; s.t=150+Math.floor(Math.random()*220);
      }
    }
    ctx.globalAlpha=1;
    requestAnimationFrame(frame);
  }
  frame();
})();
```

---

## Шаблон страницы (base)

```html
<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Workflow — Название страницы</title>
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;700;800&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@tabler/icons-webfont@2.44.0/tabler-icons.min.css">
<style>
  * { box-sizing: border-box; margin: 0; padding: 0; }
  body { font-family: 'DM Sans', sans-serif; background: #07070E; color: #fff; min-height: 100vh; }
  /* стили страницы */
</style>
</head>
<body>

<canvas id="bgCanvas" style="position:fixed;top:0;left:0;width:100%;height:100%;z-index:0;pointer-events:none;"></canvas>

<!-- НАВБАР -->
<!-- КОНТЕНТ -->

<script>/* canvas код сюда */</script>
</body>
</html>
```

---

## Компоненты

### Навбар (тёмная тема)
```html
<nav style="
  position:fixed; top:0; left:0; right:0; z-index:100;
  display:flex; align-items:center; justify-content:space-between;
  padding:18px 44px;
  background:rgba(7,7,14,0.85);
  border-bottom:0.5px solid rgba(255,255,255,0.06);
  backdrop-filter:blur(12px);
">
  <div style="font-family:'Syne',sans-serif;font-weight:800;font-size:22px;color:#fff;letter-spacing:-0.5px;">
    work<span style="color:#7F77DD">flow</span>
  </div>
  <div style="display:flex;gap:28px;">
    <a href="#" style="color:rgba(255,255,255,0.4);text-decoration:none;font-size:14px;">Проекты</a>
    <a href="#" style="color:rgba(255,255,255,0.4);text-decoration:none;font-size:14px;">Таланты</a>
  </div>
  <div style="display:flex;gap:10px;">
    <button class="btn-outline">Войти</button>
    <button class="btn-primary">Начать</button>
  </div>
</nav>
```

### Кнопки
```css
.btn-primary {
  background: #7F77DD; color: #fff; border: none;
  padding: 10px 22px; border-radius: 10px;
  font-size: 14px; font-weight: 500; cursor: pointer;
  font-family: 'DM Sans', sans-serif;
  transition: opacity 0.2s;
}
.btn-primary:hover { opacity: 0.85; }

.btn-outline {
  background: transparent; color: rgba(255,255,255,0.6);
  border: 0.5px solid rgba(255,255,255,0.15);
  padding: 10px 22px; border-radius: 10px;
  font-size: 14px; cursor: pointer;
  font-family: 'DM Sans', sans-serif;
}

.btn-green {
  background: #1D9E75; color: #fff; border: none;
  padding: 10px 22px; border-radius: 10px;
  font-size: 14px; font-weight: 500; cursor: pointer;
  font-family: 'DM Sans', sans-serif;
}

.btn-danger {
  background: rgba(239,68,68,0.15); color: #F87171;
  border: 0.5px solid rgba(239,68,68,0.25);
  padding: 10px 22px; border-radius: 10px;
  font-size: 14px; cursor: pointer;
  font-family: 'DM Sans', sans-serif;
}
```

### Карточка
```css
.card {
  background: #0D0D18;
  border: 0.5px solid rgba(255,255,255,0.07);
  border-radius: 16px;
  padding: 22px;
  transition: border-color 0.2s, transform 0.2s;
}
.card:hover {
  border-color: rgba(127,119,221,0.3);
  transform: translateY(-3px);
}
```

### Инпут
```css
.input {
  width: 100%;
  background: #0D0D18;
  border: 0.5px solid rgba(255,255,255,0.1);
  border-radius: 10px;
  padding: 12px 16px;
  color: #fff;
  font-size: 14px;
  font-family: 'DM Sans', sans-serif;
  outline: none;
  transition: border-color 0.2s;
}
.input:focus { border-color: rgba(127,119,221,0.5); }
.input::placeholder { color: rgba(255,255,255,0.22); }
```

### Тег / Badge
```css
/* Фиолетовый */
.tag-purple { background: rgba(127,119,221,0.12); color: #AFA9EC; font-size: 11px; font-weight: 500; padding: 3px 11px; border-radius: 20px; }
/* Зелёный */
.tag-green  { background: rgba(29,158,117,0.12);  color: #5DCAA5; font-size: 11px; font-weight: 500; padding: 3px 11px; border-radius: 20px; }
/* Жёлтый */
.tag-amber  { background: rgba(239,159,39,0.12);  color: #EF9F27; font-size: 11px; font-weight: 500; padding: 3px 11px; border-radius: 20px; }
/* Красный */
.tag-red    { background: rgba(239,68,68,0.12);   color: #F87171; font-size: 11px; font-weight: 500; padding: 3px 11px; border-radius: 20px; }
```

### Статус проекта
```css
.status-open        { /* tag-green  */ }
.status-in_progress { /* tag-amber  */ }
.status-delivered   { /* tag-purple */ }
.status-completed   { /* tag-green  */ }
.status-disputed    { /* tag-red    */ }
```

### Переливающийся градиент (divider)
```html
<div style="width:100%;height:1px;position:relative;overflow:hidden;background:rgba(255,255,255,0.04);">
  <div style="position:absolute;inset:0;background:linear-gradient(90deg,transparent,#7F77DD 25%,#5DCAA5 50%,#AFA9EC 75%,transparent);background-size:300% 100%;animation:divSlide 3.5s linear infinite;"></div>
</div>
<style>
@keyframes divSlide{0%{background-position:300% 0}100%{background-position:-300% 0}}
</style>
```

### Аватар пользователя
```html
<!-- Инициалы -->
<div style="
  width:44px; height:44px; border-radius:50%;
  background:rgba(127,119,221,0.15); color:#AFA9EC;
  display:flex; align-items:center; justify-content:center;
  font-family:'Syne',sans-serif; font-weight:700; font-size:14px;
">АК</div>

<!-- Онлайн индикатор -->
<div style="position:relative;display:inline-block;">
  <div style="width:44px;height:44px;border-radius:50%;background:rgba(127,119,221,0.15);..."></div>
  <div style="position:absolute;bottom:1px;right:1px;width:10px;height:10px;border-radius:50%;background:#1D9E75;border:2px solid #07070E;"></div>
</div>
```

### Рейтинг звёзды
```html
<div style="display:flex;align-items:center;gap:4px;font-size:13px;color:#EF9F27;font-weight:500;">
  <i class="ti ti-star-filled"></i> 4.9
  <span style="color:rgba(255,255,255,0.28);font-size:12px;">(87)</span>
</div>
```

### Stat блок (цифра + подпись)
```html
<div>
  <div style="font-family:'Syne',sans-serif;font-size:28px;font-weight:800;color:#fff;letter-spacing:-1px;">
    12<span style="color:#7F77DD">K+</span>
  </div>
  <div style="font-size:12px;color:rgba(255,255,255,0.28);margin-top:3px;">Фрилансеров</div>
</div>
```

---

## Страницы проекта

| Файл | Описание |
|------|----------|
| `index.html` | Главная — лендинг со статистикой, проектами, фрилансерами |
| `role-select.html` | Выбор роли (Client / Freelancer) ✅ |
| `register.html` | Регистрация |
| `login.html` | Логин |
| `dashboard-client.html` | Дашборд заказчика — мои проекты, статистика |
| `dashboard-freelancer.html` | Дашборд фрилансера — заявки, доход, рейтинг |
| `projects.html` | Лента проектов с фильтрами |
| `project-detail.html` | Страница проекта — описание, заявки, чат |
| `create-project.html` | Форма создания проекта |
| `profile.html` | Профиль пользователя — портфолио, отзывы |
| `wallet.html` | Кошелёк — баланс, история транзакций |

---

## Правила для агента

1. **Всегда** подключать шрифты Syne + DM Sans
2. **Всегда** вставлять canvas со звёздами на каждую страницу
3. **Всегда** использовать фон `#07070E` в тёмной теме
4. Карточки — фон `#0D0D18`, border `rgba(255,255,255,0.07)`
5. Акцентный цвет — `#7F77DD` (фиолетовый)
6. Заголовки — шрифт `Syne`, font-weight 800
7. Весь основной текст — `DM Sans`
8. Иконки — только Tabler Icons (`ti ti-*`)
9. Никаких внешних CSS фреймворков (Bootstrap, Tailwind)
10. Все страницы — чистый HTML + CSS + JS, один файл
